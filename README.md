# Python-IDE-TFG-
An IDE I made as the final project before graduating

You can create a .exe of the application running this command if you want to try it out for yourself:
pyinstaller <module.py> --onefile --noconsole

module.py should be main.py
